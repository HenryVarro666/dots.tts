"""dots.tts 的 Gradio 应用 / Gradio application for dots.tts.

中文：基于 Gradio 的交互式 TTS Web Demo。app(构建并启动 Gradio 界面的入口)、
service(后端服务逻辑：加载模型、调用推理运行时完成合成/克隆)、constants(常量配置)、
languages(语言相关选项/映射)、default_prompts(内置示例提示词/参考)。给 fork 拥有者一个
开箱即用、可视化体验模型效果的前端。

EN: An interactive Gradio web demo for TTS — app (builds and launches the Gradio UI),
service (backend: loads the model and drives the inference runtime for synthesis/cloning),
constants (config constants), languages (language options/mappings) and default_prompts
(bundled example prompts/references).
"""
